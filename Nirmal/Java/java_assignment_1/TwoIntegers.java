public class TwoIntegers {
    public static void main(String[] args) {
        int one=26, two=50;
            if (two % one == 0)
            System.out.println("\n" + two + " is divisible by " + one);
        else
            System.out.println("\n" + two + " is not divisible by " + one);
    }
}