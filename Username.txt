veersun@rbos.co.uk

mongodb://localhost:27017

8DC49AB4AD03
Hello@2023

120927276039
AUser71
$OppT0p#
Oct#2023
sv-database1
admin
Oct#2023
port - 3306
sv-database1.cldysgcprhnh.eu-north-1.rds.amazonaws.com

https://120927276039.signin.aws.amazon.com/console

Password#15

surendiran
aug#2023
August#2023

q2_array_to_object_converter.
q3_flatten_n_dimensional_array.js

paranthesis means ()
braces means []
curly brackets{}
orcl

N1=5
n2=1
n3=1
while [ $n3 -le $n1 ]
1 <5 – yes
 n2=` expr $n2 \* $n3 `
n2=1 * =1
n3=` expr $n3 + 1 `
n3=1 + 1=2
while [ $n3 -le $n1 ]
2<5 – yes
 n2=` expr $n2 \* $n3 `
n2=1 * 2=2
n3=` expr $n3 + 1 `
n3=2 + 1=3
while [ $n3 -le $n1 ]
4<5 – yes
 n2=` expr $n2 \* $n3 `
n2=2 * =6
n3=` expr $n3 + 1 `
n3=3+ 1=4
hile [ $n3 -le $n1 ]
4<5 – yes
 n2=` expr $n2 \* $n3 `
n2=6*4=24
n3=` expr $n3 + 1 `
n3=4+ 1=5
5<5 – yes
 n2=` expr $n2 \* $n3 `
n2=24*5=120
n3=` expr $n3 + 1 `
n3=5+ 1=6
n2=120


#! /bin/bash
echo " enter first number " a
read a
echo " enter second number " b
read b
echo " enter third number " c
read c
if [[ $a -gt $b && $a -gt $c ]]
then
echo " largest number is $a "
elif [[ $b -gt $c && $b -gt $a ]]
then
echo " largest number is $b "
else
echo " largest number is $c "
fi


#! /bin/bash
read -p "enter number" number
if [ $number -lt 0 ]
then
echo "negative number"
else
echo "positive number"
fi




login form
novbar
tables
image scroller(corosil)

#another method
cat prac8.sh | while read p
do
echo $p
done
#another method
while IFS= read -r line
do
echo $line
done < prac8.sh

step 1 : install OracleDB in order to connect Nodejs or Js to oracle
username : system
password : system
DBname : orcl
port : 1521

step 2 : cmd for installing oracle - npm install oracledb --save

step 3. to make our application as server we need tool called express,handlebar and body-parser

npm install express --save
npm install express-handlebars --save
npm install body-parser --save
npm install cors --save

http:localhost:/3001/controller1/
http://localhost:3001/controller1/

create a app as below :
create a folder Hackathon_mock
get into the folder and give npm init
inside Hacathon_mock create src. Under src create html,js and asset
create index.html,indiex.js ---- which is a entry point
create app to accept User id,userFirstname,User Emailid and DOB(use proper input type)
Use proper form,form control and form groups,css from bootstrap,margin between each
(label and input box)should be 4.
Input box should be expanded to full size screen.
add buttons "add" an "Display". When add button is clicked add above data into array or 
json objext.
on display button you need to display all object data in ul format with proper css
finally add a delete icon for every sid on delete respective sid should be deleted and also add user record for every user
Create two fuctions for buttons add,display and delete. Array should be global whcih can be accessed in all functions.








