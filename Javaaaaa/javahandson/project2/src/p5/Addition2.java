package p5;
@FunctionalInterface
public interface Addition2 {

	public int cal(int x, int y);
	// public void data();
	
	//When we use @Functional interface annotation, the compiler will not allow to create more than 1 method
	// annotation is nothing but the information given to the compiler. all the annotation will start with @
	
	
}
